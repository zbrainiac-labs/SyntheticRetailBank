name: Update Local Repository and Run Sonar Scanner

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  pipeline:
    uses: zBrainiac/DataOpsBackbone/.github/workflows/dataops-pipeline.yml@main
    with:
      SOURCE_DATABASE: AAA_DEV_SYNTHETIC_BANK
      SOURCE_SCHEMA: CRM_RAW_001
      DCM_PROJECT_IDENTIFIER: AAA_DEV_SYNTHETIC_BANK.CRM_RAW_001.AAA_SYNTHETIC_BANK
      DCM_TARGET: DEV
    secrets: inherit
